# 项目上下文 — DeepSeek-V4-Flash Blackwell 部署 + 性能优化

## 项目初始需求
实现 DeepSeek-V4-Flash 在本机 RTX PRO 6000 x 4 (SM120/Blackwell) 上的部署。

## 项目元信息
- **架构**: vLLM + DeepSeek-V4-Flash 模型 + DeepGEMM SM120
- **硬件**: 4x NVIDIA RTX PRO 6000 Blackwell Server Edition (SM120, ~98GB each, 总~384GB)
- **CPU**: AMD EPYC 9654 96-Core × 2 NUMA, 192 threads, 1TB RAM
- **CUDA**: /opt/cuda-13.0.3 (CUDA 13.0.88)
- **PyTorch**: 2.11.0+cu130
- **vLLM**: ds4-sm120-preview-dev branch
- **DeepGEMM**: 2.5.0+local (sm120 branch, 已修改 layout.hpp)
- **模型**: /models/models/deepseek-ai/DeepSeek-V4-Flash/ (~313GB, 46 safetensors)
- **远端**: origin = git@github.com:CollinZhou/vllm_dsv4.git
- **git 分支**: ds4-sm120-preview-dev

## 服务状态
| INDEX | 服务名 | PID | 端口 | GPU | 状态 |
|-------|--------|-----|------|-----|------|
| 1 | DeepSeek-V4-Flash | 565630 | 8006 | 0-3 | **running** (FlashInfer 0.6.13 SM120 decode) |

## 完整任务清单
### 第一阶段：基础部署
- [x] 1. Git fork 设置和 upstream 同步
- [x] 2. vLLM ds4-sm120-preview-dev 分支切换
- [x] 3. vLLM 环境安装 (uv, pip install, modelscope)
- [x] 4. DeepGEMM sm120 分支编译
  - [x] layout.hpp transform_sf_into_required_layout: arch_major == 10 → >= 10
  - [x] layout.hpp transform_k_grouped_sf_into_required_layout: arch_major == 10 → >= 10
  - [x] utils/layout.hpp get_default_recipe: 添加 arch_major == 12 case
  - [x] DeepGEMM 重新编译安装完成
- [x] 5. vLLM 侧 SM120 支持
  - [x] support_deep_gemm() 添加 is_device_capability_family(120)
  - [x] get_default_recipe SM120 支持 (commit 08e61699e)
  - [x] DeepGEMM FP8 GEMM 跳过 (fallback to torch._scaled_mm)
- [x] 6. 完整推理测试 — 成功

### 第二阶段：性能优化
- [x] 7. [P0] Fill SM120 all_reduce_utils tables
- [x] 8. [P0] Fill SM120 symm_mem world sizes
- [x] 9. [P0] Fill SM120 allreduce_rms_fusion tables
- [x] 10. [P0] Fill SM120 sequence_parallelism tables
- [x] 11. [P1] Optimize start script + NCCL + FlashInfer SM120
- [x] 12. [P1] Restart & Benchmark optimized config
- [x] [P2] Upgrade flashinfer to >= 0.6.13 for SM120 decode kernel — DONE
- [x] [P2] Enable SM120 decode + allreduce fusion, benchmark — DONE
- [x] [P2] Long context stability test (>100k tokens) — DONE
- [ ] [P2] Investigate cross-NUMA TP=6 or dual-instance

## 已完成的核心功能
### 第一阶段：基础部署
- DeepGEMM sm120 分支编译成功
- vLLM 已识别 SM120 DeepGEMM 支持
- 模型权重加载成功 (46 safetensors)
- FP8 KV-cache 配置正确
- SM120 专用 attention 优化已集成
- **推理服务成功运行**（TP=4, GPU 0-3, 端口 8006）

### 第二阶段：性能优化 (V3 — FlashInfer 0.6.13)
- **FlashInfer 升级到 0.6.13 (main/nightly)**，源码安装 (commit 9c5ed7c1)
- **SM120 decode 已启用** (`VLLM_DEEPSEEK_V4_FLASHINFER_SM120_DECODE=1`)
- **FlashInfer allreduce fusion 保持禁用**（PCIe 上依然不稳定）
- **FLASHINFER_DISABLE_VERSION_CHECK=1**（cubin 0.6.12 兼容）
- Decode 基准: 85.7 tok/s 单请求, 283.7 tok/s 4并发
- 长上下文: 264K tokens 通过，KV cache 峰值 6.4%

## 性能基线 (V3)
| 指标 | V2 (FlashMLA) | V3 (FlashInfer SM120) | 提升 |
|------|--------------|----------------------|------|
| Decode（单请求） | ~82 tok/s | 85.7 tok/s | +4.5% |
| Decode（4并发聚合） | ~280 tok/s | 283.7 tok/s | +1.3% |
| Prefill 峰值 | ~15,000 tok/s | ~13,816 tok/s | -7.9% |
| Long context (max tested) | — | 264K tokens ✅ | — |

## 关键代码/文件位置
- `vllm/distributed/device_communicators/all_reduce_utils.py:31-70` — SM120 配置表
- `vllm/distributed/device_communicators/symm_mem.py:26-31` — World sizes
- `vllm/compilation/passes/fusion/allreduce_rms_fusion.py:91-128` — FlashInfer fusion sizes
- `vllm/compilation/passes/fusion/sequence_parallelism.py:40-53` — SP thresholds
- `vllm/models/deepseek_v4/nvidia/model.py` — DeepSeek V4 模型实现
- `vllm/models/deepseek_v4/attention.py` — Attention 实现
- `vllm/models/deepseek_v4/nvidia/flashinfer_sm120_decode.py` — SM120 decode 路径
- `flashinfer/flashinfer/mla/_sparse_mla_sm120.py` — FlashInfer 0.6.13 SM120 decode kernel
- `flashinfer/flashinfer/jit/mla.py` — gen_sparse_mla_sm120_module() JIT 入口
- `start_stop_dsv4/1_start_deepseekv4_vllm.sh` — 启动脚本 V2
- `start_stop_dsv4/stop_1_deepseekv4_vllm.sh` — 停止脚本

## 参考部署方案 (来自 docs/vllm_dsv4.docx)
当 DeepGEMM FP8 GEMM kernel 不可用时，可以使用 SM120 reference 路径绕过：
```bash
export VLLM_SM120_REFERENCE_DEEPSEEK_V4_ATTENTION=1
export VLLM_SM120_REFERENCE_TOPK_CHUNK_SIZE=256
export VLLM_SM120_REFERENCE_QUERY_CHUNK_SIZE=128
export VLLM_ATTENTION_BACKEND=FLASH_ATTN_2
```

## 部署命令 (4 GPU, 适配本机)
```bash
export CUDA_HOME=/opt/cuda-13.0.3
export PATH=/opt/cuda-13.0.3/bin:$PATH
export TRITON_PTXAS_PATH=/opt/cuda-13.0.3/bin/ptxas
export CUDA_VISIBLE_DEVICES=0,1,2,3
export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True

vllm serve /models/models/deepseek-ai/DeepSeek-V4-Flash/ \
  --trust-remote-code \
  --kv-cache-dtype fp8 \
  --block-size 256 \
  --tensor-parallel-size 4 \
  --tokenizer-mode deepseek_v4 \
  --tool-call-parser deepseek_v4 \
  --enable-auto-tool-choice \
  --reasoning-parser deepseek_v4 \
  --host 0.0.0.0 \
  --port 8000 \
  --served-model-name VLLM-MODEL \
  --gpu-memory-utilization 0.95 \
  --max-num-seqs 4 \
  --max-model-len 32768 \
  --compilation-config '{"cudagraph_mode":"FULL_AND_PIECEWISE", "custom_ops":["all"]}' \
  --async-scheduling \
  --enable-prefix-caching \
  --load-format auto
```

## 未解决的问题
1. **FlashInfer SM120 decode path 不可用** — 需要升级到 >= 0.6.13
2. **FlashInfer allreduce fusion 在 PCIe+0.6.12 上不稳定** — NCCL worker 崩溃
3. **Custom allreduce 被 PCIe gate 阻断** — >2 PCIe GPUs 检查无法绕过，需要源码修改 force-enable
4. **TileLang 编译 5-7s 毛刺** — `mhc_pre_big_fuse_with_norm_tilelang` 在每个不同形状请求上重新编译
5. **跨 NUMA 性能问题** — GPU0-3 在 NUMA0, GPU4-5 在 NUMA1, GPU0-1 PIX 但 0-2/1-3 跨 NODE

## 下一步计划
1. 升级 flashinfer 到 >= 0.6.13（源码编译或 wheel）
2. 重试 FlashInfer SM120 decode + allreduce fusion
3. 长上下文稳定性测试（100K+ tokens）
4. 研究跨 NUMA TP=6 的可行性

## 已尝试的技术路线
### 第一阶段
1. **C++ 侧 arch_major >= 10 dispatch** — 不可行，SM120 没有对应的 FP8 GEMM kernel 实现
2. **Python 侧 is_deep_gemm_supported() 返回 False** — fallback 到 torch._scaled_mm
3. **get_default_recipe() 添加 SM120** — 已修复，避免 "Unknown recipe" 断言错误
4. **PyTorch 2.12.1 升级** — NCCL version mismatch，已回滚

### 第二阶段
5. **SM120 配置填充** — 成功，解锁 prefill 3x
6. **NCCL manual override** — 失败，decode 从 82→73 tok/s 退化
7. **FlashInfer allreduce fusion** — 失败，worker 崩溃
8. **FlashInfer SM120 decode** — 延迟到升级后

## 操作规范
- 启动服务必须 cd 到 start_stop_dsv4/ 再执行脚本（日志和 PID 文件写入正确目录）
- 停止服务始终用 stop script，不要手动 kill
- 端口分配：8000-8005 生产，8006-8009 实验
